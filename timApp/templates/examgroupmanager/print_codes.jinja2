<!DOCTYPE html>
<html lang='{{ locale }}'>
<head>
    <meta charset='UTF-8'>
    <meta name='viewport'
          content='width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0'>
    <meta http-equiv='X-UA-Compatible' content='ie=edge'>
    <title>{{ title }}</title>
    <link href='/static/images/favicon.ico' rel='shortcut icon' type='image/x-icon'/>
    <style>
        @page {
            size: A4;
            margin: 0.5cm;
        }

        * {
            font-family: Arial, Helvetica Neue, Helvetica, sans-serif;
        }

        section {
            display: flex;
            flex-wrap: wrap;
            padding: 0;
        }

        .student-card {
            padding: 0.3em;
            outline: 1px dashed black;
            margin-top: 1px;
            margin-left: 1px;

            display: flex;
            flex-direction: column;
        }


        .code {
            justify-self: center;
            font-family: monospace;
            font-size: 2em;
        }

        .name-title, .url-title, .code-title {
            font-weight: bold;
            font-size: 0.8em;
            margin-top: 0.8em;
            margin-bottom: 0.3em;
        }

        .code-title {
            align-self: center;
        }

        .student-card > *:last-child {
            margin-bottom: 0;
        }

        .student-card > *:first-child {
            margin-top: 0;
        }

    </style>
</head>
<body>
<main>
    <h1>{{ title }}</h1>
</main>
<section>
    {% for user in users %}
        <div class="student-card">
            <div class="name-title" style="grid-area: name-title;">{{ gettext("Name") }}</div>
            <div class="name" style="grid-area: name;">{{ user.real_name }}</div>
            <div class="url-title" style="grid-area: url-title;">{{ gettext("Exam address") }}</div>
            <div class="url" style="grid-area: url;">{{ exam_url }}</div>
            <div class="code-title" style="grid-area: code-title;">{{ gettext("Login code") }}</div>
            <div class="code"
                 style="grid-area: code;">{{ user.login_code | slice(3) | join("-") | replace("'","") | replace(",","") | replace("[","") | replace("]","") | replace(" ","") }}</div>
        </div>
    {% endfor %}
</section>
</body>
</html>